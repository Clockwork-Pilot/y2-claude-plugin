# Constraint Scripts Directory

This directory contains reusable constraint validation scripts that can be used in Task feature constraints.

## Structure
- Store constraint scripts here (bash, python, etc.)
- Reference them from constraint definitions using relative paths or ${PROJECT_ROOT}
- Scripts should be self-contained and executable
- Document expected input/output for each script

